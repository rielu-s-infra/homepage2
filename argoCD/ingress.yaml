apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rielu-site3-combined-ingress
  namespace: rielu-site3
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # ここには rewrite-target を書かない（グローバルに適用されるのを防ぐ）
spec:
  ingressClassName: nginx
  rules:
    - host: rieluoff.uniproject.jp
      http:
        paths:
          # フロントエンド（rewriteなし：/assets/xxx がそのまま届く）
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rielu-site3
                port:
                  number: 3000
  tls:
    - hosts:
        - rieluoff.uniproject.jp
      secretName: rieluoff-uniproject-jp-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rielu-site3-api-ingress
  namespace: rielu-site3
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host: rieluoff.uniproject.jp
      http:
        paths:
          # API（ここだけ rewrite あり）
          - path: /api-kuma(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: uptime-kuma-service
                port:
                  number: 80