name: rielu-site3
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DISCORD_BOT_TOKEN: "YourToken"
      DISCORD_GUILD_ID: "YourGuildID"
      DISCORD_TARGET_ROLE_ID: "YourTargetRoleID"
    networks:
      - rielu-site3-network

networks:
  rielu-site3-network:
    driver: bridge
